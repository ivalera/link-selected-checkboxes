{
  "name": "link-selected-checkboxes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "jsdom": "^26.0.0",
    "vite": "^6.0.5",
    "vitest": "^3.0.3"
  }
}
